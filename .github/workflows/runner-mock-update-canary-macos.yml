# This is a basic workflow to help you get started with Actions

name: runner-mock-update-canary-macos
on:
  [workflow_dispatch, workflow_call]
jobs:
  build:
    runs-on: [self-hosted, macos]
    steps:
      - name: Run a one-line script
        timeout-minutes: 1
        run: echo Hello, world!
